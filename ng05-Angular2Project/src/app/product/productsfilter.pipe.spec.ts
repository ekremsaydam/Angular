import { ProductsfilterPipe } from './productsfilter.pipe';

describe('ProductsfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
